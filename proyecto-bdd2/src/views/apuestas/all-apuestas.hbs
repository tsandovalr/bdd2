<div class="row row-cols-1 row-cols-md-3 g-4 ">
    {{#each apuestas}}
    <div class="col-md-4 mt-5 mb-5">
        <div class="card h-100 ">
            
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{title}} <a href="/apuestas/edit"><i class="fas fa-user-edit"></i></a>
                    </h4>
                   
                <p>id_apuesta: {{id_apuesta}}</p>
                <p>id_partido: {{id_partido}} bsf <i class="fas fa-money-bill-alt"></i> </p>
                <p>monto_apuesta: {{monto_apuesta}}</p>
                <P>opción: {{opcion}}</P>
                <p>usuario: {{usuario}}</p>
                <div class="card-footer">
                <form action="/apuestas/delete/?_method=DELETE" method="POST">
                <button type="submit" class="btn btn-outline-danger btn-sm">
                    Delete <i class="fas fa-trash-alt"></i>
                </button>
                </form>
                </div>
            </div>
         </div>
      </div>

    {{/each}}
    <div class="card mx-auto mt-5 mb-5">
        <div class="card-body">
            <p class="lead">Tienes ganas de ganar billullo?</p>
            <a href="/apuestas/add" class="btn btn-success btn-block">Apuesta!</a>

        </div>

    </div>
</div>